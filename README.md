Snake game written in C, compiled via WebAssembly.

# Controls

* Move: arrow keys or WASD
* Pause/unpause: P

# Building

Requires clang and wasm-ld

`npm run build`

# Serve locally

Requires python3

`npm run serve`
